<div class="card">
    <div class="card-header">
        <h5 class="card-title mb-0"><%= "Editar #{@category.description}" %></h5>
    </div>
    <div class="card-body">
        <%= render 'form', category: @category %>
    </div>
</div>
